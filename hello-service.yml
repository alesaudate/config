message: "Victor!"
spring:
  application:
   name: hello-service
eureka:
  instance:
    hostname: http://eureka-server-net.cfapps.io/
    nonSecurePort: 80
    metadataMap:
      instanceId: ${vcap.application.instance_id:${spring.application.instance_id:${server.port}}}