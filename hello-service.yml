message: "Olá mundo!"
spring:
  application:
   name: hello-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server-demo.cfapps.io/
  instance:
    leaseRenewalIntervalInSeconds: 10
    metadataMap:
      instanceId: ${vcap.application.instance_id:${spring.application.name}:${server.port:8080}}
      
